[project]
name = "cours-lasalle"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kuurkuma", email = "t.ber@mailfence.com" }
]
requires-python = ">=3.13"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyterlab>=4.6.4",
    "matplotlib>=3.10",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
]

[project.scripts]
cours-lasalle = "cours_lasalle:main"

[build-system]
requires = ["uv_build>=0.12.18,<0.13.0"]
build-backend = "uv_build"
